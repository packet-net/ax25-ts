{
  "$comment": "Reviewed, intentional divergences from the C# reference (m0lte/packet.net). Every entry is a decision with a reason — scripts/parity-check.mjs treats anything NOT listed here as drift and fails CI. Remove an entry when the TS counterpart lands. Scope note: the listener-surface comparison covers C# public METHODS and EVENTS; C# read-only properties (MyCall, IsRunning, ActiveSessions, CurrentSessionParameters, AcceptIncoming) are deliberately outside the extractor — they are inspection conveniences, not behaviour.",
  "parseOptionFlags": {},
  "parsePresets": {},
  "quirkFlags": {},
  "quirkPresets": {},
  "xidFlags": {},
  "listenerOptions": {
    "RestartT1OnTxComplete": "TX-complete→T1: re-arms a running T1 from the TNC's ACKMODE TX-completion echo (packet.net kiss.t1FromTxComplete). The echo is a TNC/node-host transport capability; no TS transport implements SendFrameWithAckAsync today, so the option would be inert. Add the TS leg when an ACKMODE-capable TS transport exists.",
    "PortName": "Human-readable label for the modem/port a listener is attached to (\"kiss-tcp:192.168.1.10:8001\", \"serial:/dev/ttyUSB0\"), included in packet.net's structured debug log lines so an operator running several ports can tell which modem a message came from. A diagnostics/logging label with no on-air or FSM behaviour — same class as the FrameTraced listener-surface exception. A TS consumer labels its own logs; add a TS leg only if @packet-net/ax25 grows multi-port structured logging."
  },
  "listenerSurface": {
    "UpdateSessionParameters": "Live per-listener reseed is driven by packet.net's node-host hot-reload; browser consumers reconstruct the listener instead. Revisit if a TS host needs hot reconfig.",
    "AddLocalAlias": "Multi-callsign acceptance (the node answers for app callsigns registered by packet.net's RHPv2 server, e.g. a DAPPS instance's callsign). Node-host capability; a browser consumer constructs one listener per identity instead. TS leg tracked as a follow-up if a TS host needs it.",
    "RemoveLocalAlias": "Pair of AddLocalAlias — see that entry."
  }
}
